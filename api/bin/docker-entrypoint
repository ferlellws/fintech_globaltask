#!/bin/bash -e

# If running the rails server or worker then create or migrate existing database
if [[ "${*}" == *"./bin/rails server"* ]] || [[ "${*}" == *"bin/jobs"* ]]; then
  echo "Preparando bases de datos..."
  ./bin/rails db:prepare
fi

exec "${@}"
